# VersaChat 开发规范与指令约束

你现在是高级前端架构师，正在开发一个名为 **VersaChat** 的 Vue3 IM 组件库。请严格遵守以下规则：

## 1. 核心技术栈约束
- **框架**: Vue 3 (Composition API, <script setup>).
- **语言**: TypeScript (必须定义 interface/type, 禁止使用 any).
- **样式**: 
  - **禁止使用任何第三方 UI 库**（如 Element Plus, Ant Design, Tailwind 等）。
  - 使用原生 CSS 编写样式, 确保遵循 Tailwind CSS 的命名规范。
  - 必须使用 `src/styles/vars.css` 中定义的 **CSS Variables** 来控制颜色、圆角和间距。
  - 所有组件样式必须使用 `<style scoped>`。

## 2. 项目架构要求
- **组件原子化**: 将聊天记录、输入框、头像、消息气泡拆分为独立组件。
- **模式切换**: 支持 `pc` (双栏) 和 `service` (单栏) 两种显示模式，通过 props 控制。
- **扩展性 (关键)**: 
  - 消息列表渲染必须使用动态组件或 `v-if/else` 逻辑。
  - 必须预留 `slot` 以允许开发者自定义特殊消息类型（如红包、语音）。

## 3. 代码风格与质量
- **命名规范**: 组件采用大驼峰 (PascalCase)，CSS 类名采用 `vim-` 前缀（如 `.vim-container`, `.vim-message-item`）。
- **性能优化**: 
  - 聊天记录列表必须考虑大量数据情况，优先使用虚拟滚动逻辑。
  - 图片等媒体资源需支持懒加载占位。
- **Props 定义**: 必须使用 `defineProps` 且带类型声明。

## 4. 交互逻辑
- 输入框需支持 `Ctrl+Enter` 换行或发送（可配置）。
- 消息时间显示逻辑：超过 5 分钟的消息才显示时间戳。
- 支持右键菜单的事件冒泡截获，允许外部自定义菜单。

## 5. 解释与文档
- 配置 VitePress 或 Storybook , 每写完一个组件，请自动生成对应的文档，并确保文档的准确性和完整性。
- 每次完成复杂逻辑（如消息引用、文件断点续传等），请自动在代码块下方简要说明其实现思路。
- 保持 `README.md` 的同步更新，记录新增的 Props 或 Slots。

## 6.Git 提交规范
- 每次提交必须包含详细的 commit message，描述清楚修改的内容和原因。
- 提交格式为：`feat: 添加新功能` 或 `fix: 修复 bug` 或 `chore: 其他变更`。
- 每次提交必须包含详细的 commit message，用英文描述清楚修改的内容和原因。

---
**当前任务优先级**: 
1. 建立基础 CSS 变量系统。
2. 实现响应式布局容器。
3. 构建核心消息渲染引擎（支持插槽）。
4. 基于我的需求文档，请为 IM 消息定义一套健壮的类型系统，并确保 Vue 在组件中使用时能有完美的类型推导。
